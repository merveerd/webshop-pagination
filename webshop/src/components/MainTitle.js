import React from "react";
import styled from "styled-components";

const Title = styled.h4`
  color: #6f6f6f;
  margin: 0;
  font-family: Open Sans;
  font-weight: normal;
  font-size: 20px;
  letter-spacing: 0.25px;
`;

const MainTitle = () => <Title>Products</Title>;

export { MainTitle };
